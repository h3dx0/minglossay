{% extends 'base.html.twig' %}

{% block body %}
    <h1>Miniglossary</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ miniGlossary.id }}</td>
            </tr>
            <tr>
                <th>Topic</th>
                <td>{{ miniGlossary.topic }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ miniGlossary.description }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('miniglossary_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('miniglossary_edit', { 'id': miniGlossary.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
