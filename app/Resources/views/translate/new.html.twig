{% extends 'base.html.twig' %}

{% block body %}
<h1>Translate creation</h1>
<div class="row">
    <div class="card col-6 mx-auto">
        <div class="card-body">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <input type="submit" class="btn btn-success mt-3" value="Create" />
            {{ form_end(form) }}

        </div>
    </div>

</div>

<ul>
    <li>
        <a href="{{ path('translate_index') }}">Back to the list</a>
    </li>
</ul>
{% endblock %}
