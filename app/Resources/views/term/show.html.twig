{% extends 'base.html.twig' %}

{% block body %}
    <h1>Term</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ term.id }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ term.text }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('term_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('term_edit', { 'id': term.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
